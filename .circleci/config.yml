version: '2.1'
orbs:
  android: circleci/android@2.0
workflows:
  test:
    jobs:
      - android/run-ui-tests:
          executor:
            name: android/android-machine
            resource-class: xlarge
